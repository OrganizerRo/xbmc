name: Build Windows

on:
  push:
    branches:
      - Krypton
  pull_request:
    branches:
      - Krypton
  workflow_dispatch:

jobs:
  build-kodi:
    name: Build Kodi (Windows x86)
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # Cache the MinGW-built FFmpeg and libdvd trees (includes renamed .lib files).
      # Skips the entire MinGW build on cache hits; only invalidated when source
      # version files or build scripts change.
      - name: Cache MinGW-built libraries
        uses: actions/cache@v4
        id: mingw-cache
        with:
          path: |
            lib/win32/ffmpeg
            lib/libdvd
          key: mingw-libs-${{ hashFiles('tools/depends/target/ffmpeg/FFMPEG-VERSION',
                                        'tools/depends/target/libdvdcss/DVDCSS-VERSION',
                                        'tools/depends/target/libdvdread/DVDREAD-VERSION',
                                        'tools/depends/target/libdvdnav/DVDNAV-VERSION',
                                        'tools/buildsteps/win32/make-mingwlibs.sh',
                                        'tools/buildsteps/win32/buildhelpers.sh',
                                        'tools/buildsteps/win32/buildffmpeg.sh',
                                        'tools/buildsteps/win32/buildlibdvd.sh',
                                        'tools/buildsteps/win32/fmpeg_options.txt') }}

      # ---------------------------------------------------------------
      # The steps below only run when the MinGW cache is cold.
      # ---------------------------------------------------------------

      - name: Add MSYS2 to PATH
        if: steps.mingw-cache.outputs.cache-hit != 'true'
        shell: cmd
        run: |
          echo C:\msys64\usr\bin>>%GITHUB_PATH%
          echo C:\msys64\bin>>%GITHUB_PATH%

      - name: Update MSYS2 package database
        if: steps.mingw-cache.outputs.cache-hit != 'true'
        shell: cmd
        run: bash -lc "pacman --needed --noconfirm -Sy"

      # Mount the workspace as /xbmc inside MSYS2 so the build scripts
      # (which hard-code /xbmc/ paths) work without modification.
      - name: Mount workspace as /xbmc in MSYS2
        if: steps.mingw-cache.outputs.cache-hit != 'true'
        shell: cmd
        run: mklink /j C:\msys64\xbmc %GITHUB_WORKSPACE%

      - name: Link MSYS2 into BuildDependencies
        if: steps.mingw-cache.outputs.cache-hit != 'true'
        shell: cmd
        run: mklink /j %GITHUB_WORKSPACE%\project\BuildDependencies\msys64 C:\msys64

      - name: Install MinGW32 build tools
        if: steps.mingw-cache.outputs.cache-hit != 'true'
        shell: cmd
        run: >
          bash -lc "pacman --needed --noconfirm -S
          base-devel
          git
          wget
          mingw-w64-i686-gcc
          mingw-w64-i686-binutils
          mingw-w64-i686-pkg-config
          mingw-w64-i686-gnutls
          mingw-w64-i686-yasm"

      # Create the /build and /local32 directories that make-mingwlibs.sh
      # expects, and write the minimal profile.local it sources at runtime.
      - name: Set up MinGW32 build environment
        if: steps.mingw-cache.outputs.cache-hit != 'true'
        shell: cmd
        run: |
          mkdir C:\msys64\build
          mkdir C:\msys64\local32\etc
          bash -c "printf 'export BITS=32bit\nexport LOCALBUILDDIR=/build\nexport LOCALDESTDIR=/local32\n' > /local32/etc/profile.local"

      # Build FFmpeg and libdvd using the existing Kodi build scripts.
      # MSYSTEM=MINGW32 makes bash --login set up the i686-w64-mingw32 toolchain.
      # Output locations (matching what ArchSetup.cmake adds to CMAKE_SYSTEM_PREFIX_PATH):
      #   FFmpeg  -> lib/win32/ffmpeg/{include,lib,bin}
      #   libdvd  -> lib/libdvd/{include,lib,bin}  (dynamically loaded on Windows)
      #   DLLs    -> system/  (copied by the build scripts)
      - name: Build FFmpeg and libdvd from source
        if: steps.mingw-cache.outputs.cache-hit != 'true'
        shell: cmd
        env:
          MSYSTEM: MINGW32
        run: >
          bash --login
          /xbmc/tools/buildsteps/win32/make-mingwlibs.sh
          --prompt=noprompt
          --mode=clean
          --build32=yes
          --build64=no

      # Validate that the MinGW build produced expected output directories and
      # files before the cache is written.  Catches silent build failures early
      # with a clear error message instead of a cryptic CMake "cannot find"
      # error on the next cache hit.
      - name: Validate MinGW build outputs
        if: steps.mingw-cache.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $required = @(
            "lib\win32\ffmpeg\lib",
            "lib\win32\ffmpeg\include",
            "lib\libdvd\lib",
            "lib\libdvd\include"
          )
          $failed = $false
          foreach ($rel in $required) {
            $path = "$env:GITHUB_WORKSPACE\$rel"
            if (-not (Test-Path $path) -or (Get-ChildItem $path).Count -eq 0) {
              Write-Error "Missing or empty: $path"
              $failed = $true
            }
          }
          if ($failed) { exit 1 }

      # MinGW produces lib*.dll.a import libs; MSVC needs *.lib names.
      # Strip the "lib" prefix and replace ".dll.a" with ".lib".
      - name: Rename MinGW FFmpeg import libs for MSVC
        if: steps.mingw-cache.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $libDir = "$env:GITHUB_WORKSPACE\lib\win32\ffmpeg\lib"
          if (Test-Path $libDir) {
            $files = Get-ChildItem -Path $libDir -Filter "lib*.dll.a"
            if ($files.Count -eq 0) {
              Write-Error "No lib*.dll.a import libraries found in $libDir — FFmpeg build likely failed or output went to wrong location"
              exit 1
            }
            $files | ForEach-Object {
              $newName = $_.Name.Substring(3) -replace '\.dll\.a$', '.lib'
              Rename-Item $_.FullName (Join-Path $_.DirectoryName $newName)
            }
          }

      # ---------------------------------------------------------------
      # These steps always run (cache hit or miss).
      # ---------------------------------------------------------------

      # On a cache hit the DLLs are not yet in system/ (the build scripts
      # put them there, but that ran in a previous job).  Copy them now.
      - name: Copy MinGW DLLs to system directory
        shell: pwsh
        run: |
          $systemDir = "$env:GITHUB_WORKSPACE\system"
          New-Item -ItemType Directory -Force $systemDir | Out-Null
          $ffmpegBin = "$env:GITHUB_WORKSPACE\lib\win32\ffmpeg\bin"
          $dvdBin    = "$env:GITHUB_WORKSPACE\lib\libdvd\bin"
          if (Test-Path $ffmpegBin) { Copy-Item "$ffmpegBin\*.dll" $systemDir -Force }
          if (Test-Path $dvdBin)    { Copy-Item "$dvdBin\*.dll"    $systemDir -Force }

      # Downloads all other pre-built Windows deps (curl, openssl, sqlite, etc.)
      # from mirrors.kodi.tv into project/BuildDependencies/{lib,include}.
      - name: Download build dependencies
        shell: cmd
        working-directory: project/BuildDependencies
        run: DownloadBuildDeps.bat

      - name: Configure with CMake
        shell: cmd
        run: |
          mkdir kodi-build
          cd kodi-build
          cmake -G "Visual Studio 17 2022" -A Win32 ..\project\cmake

      - name: Build
        shell: cmd
        working-directory: kodi-build
        run: cmake --build . --target ALL_BUILD --config RelWithDebInfo

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kodi-windows-x86
          path: |
            kodi-build/RelWithDebInfo/
            system/
          if-no-files-found: warn
